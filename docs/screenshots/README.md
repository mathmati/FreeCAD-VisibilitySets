# Screenshots

Screenshots from `verify/drivers/visibility_driver.py` land here once that
driver has actually been run. As of 2026-07-18 the driver is UNVERIFIED
(written, never executed; no GUI session was available on the build
machine), so this directory is intentionally empty.
